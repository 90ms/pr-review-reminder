display_information:
  name: PR Review Issue Worker
  description: Approve Codex implementation of labeled GitHub issues
features:
  bot_user:
    display_name: PR Review Issue Worker
    always_online: true
oauth_config:
  scopes:
    bot:
      - chat:write
settings:
  interactivity:
    is_enabled: true
  org_deploy_enabled: false
  socket_mode_enabled: true
  token_rotation_enabled: false
